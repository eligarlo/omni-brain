# Omni Brain

## Backend Setup

1. Install dependencies:
  `cd api && pip install -r requirements.txt`
2. Update the `.env` file with your OpenAI and Langfuse credentials.
3. Start the server:
  `fastapi dev`
